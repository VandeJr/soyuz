import (
    { StringExtensions } from "@soyuz/string"
    { Position, TokenType, Token } from "lib/lexer/tokens"
    { lookupIdent } from "lib/lexer/keywords"
)

pub record Lexer {
    input: String,
    ch: Char,
    line: Int,
    column: Int,
    position: Int,
    readPosition: Int
}
