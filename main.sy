import (
    { runCliFromArgv, defaultCliProgramArgv } from @driver/main_entry
)

fn main() {
    // Standalone binary: use default argv until prelude args() codegen works in file mode.
    print(runCliFromArgv(defaultCliProgramArgv()))
}
